# HTTP transport — FastAPI + Mangum (Vercel serverless adapter)
fastapi>=0.110.0,<1.0.0
mangum>=0.17.0
uvicorn>=0.29.0            # local dev server (not used by Vercel, but needed for `uvicorn main:app`)

# LangChain — pinned to 0.2.x series where AgentExecutor is stable
# langchain-community 0.2.19 requires langchain>=0.2.17
langchain>=0.2.17,<0.3.0
langchain-core>=0.2.38,<0.3.0
langchain-community>=0.2.17,<0.3.0
langchain-openai>=0.1.20,<0.2.0

# Langfuse observability
langfuse>=2.0.0,<3.0.0

# HTTP client (used by Auth0 JWKS fetching and SerpAPI)
requests>=2.31.0
httpx>=0.27.0

# Auth0 JWT validation
python-jose[cryptography]>=3.3.0

# Environment management
python-dotenv>=1.0.1

# Data validation (used by agent tools internally)
pydantic>=2.0.0,<3.0.0

# SerpAPI Python client
google-search-results>=2.4.0
