# Test dependencies for the Job Placement Agent backend
# Install with: pip install -r requirements-test.txt

# Testing framework
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
pytest-asyncio>=0.23.0

# gRPC — needed to import grpc module in tests (grpc_server.py uses it)
grpcio>=1.62.0,<2.0.0

# FastAPI — needed for auth0.py dependency helpers (Security, HTTPBearer) used in tests
fastapi>=0.100.0

# HTTP testing and type stubs
httpx>=0.27.0
types-requests>=2.31.0
