# FinDoc Analyzer — Python Dependencies
# Install: pip install -r requirements.txt

# ── Web Framework ─────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── LlamaIndex Core (Sprint4) ─────────────────────────────────────
llama-index-core==0.11.0
llama-index-vector-stores-postgres==0.2.0
llama-index-agent-openai==0.3.0

# ── LLM Providers ─────────────────────────────────────────────────
# Azure OpenAI (default: LLM_PROVIDER=azure)
llama-index-llms-azure-openai==0.2.2
llama-index-embeddings-azure-openai==0.2.2

# Anthropic / Claude (GAP FIX: LLM_PROVIDER=anthropic)
anthropic==0.34.2
llama-index-llms-anthropic==0.3.0

# OpenAI Direct (LLM_PROVIDER=openai)
openai==1.47.0
llama-index-llms-openai==0.2.10
llama-index-embeddings-openai==0.2.5

# ── Database ──────────────────────────────────────────────────────
psycopg[binary]==3.2.1
psycopg2-binary==2.9.9
sqlalchemy==2.0.35
pgvector==0.3.2

# ── Document Parsing (Sprint5 / Gap Fix) ─────────────────────────
pymupdf==1.24.10
pandas==2.2.2
openpyxl==3.1.5

# ── Evaluation & Observability (Sprint9 / Partial Fix) ───────────
langfuse==2.49.0
ragas==0.1.21
datasets==2.21.0

# ── Safety & Guardrails (Sprint6 / Partial Fix) ──────────────────
# guardrails-ai — install SDK then pull validators from Hub:
#   pip install guardrails-ai
#   guardrails hub install hub://guardrails/detect_pii
#   guardrails hub install hub://guardrails/toxic_language
guardrails-ai==0.5.10

# Presidio PII detection (optional but recommended)
#   After install: python -m spacy download en_core_web_lg
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.6

# ── MCP Tools (Sprint8) ───────────────────────────────────────────
# llama-index-tools-mcp  # uncomment when available in your env
huggingface-hub==0.24.6

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.9.2
httpx==0.27.2

# ── Development & Testing ─────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
