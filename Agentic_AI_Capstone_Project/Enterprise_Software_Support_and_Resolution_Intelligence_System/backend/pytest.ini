[pytest]
markers =
    eval: real LLM calls, run manually, not part of PR-blocking CI
    vcr: L3 integration test replayed from a recorded tests/cassettes/ fixture (§19) — real call shape, no live call on replay
